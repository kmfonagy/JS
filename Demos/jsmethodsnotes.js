//Adding methods to an object
//Namespacing
//The Keyword "this"

var obj = {
    name: "Name",
    age: 18,
    isCool: false,
    friends: ["Name1", "Name2"],
    add: function(x,y){
        return x + y;
    }
}

obj.add(10,5);
15

//same as
console.log()

//Why to add
//1 keeps things clean & together